# Root-slice swap protection: when total swap exceeds oomd's SwapUsedLimit,
# kill the cgroup using the most swap. This directly prevents the swap-exhaustion
# OOM cascade we hit (free swap was 136kB at the kill). Mirrors Fedora's default.
[Slice]
ManagedOOMSwapUsedLimit=kill
