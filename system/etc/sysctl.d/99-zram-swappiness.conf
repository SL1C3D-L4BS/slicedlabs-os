# zram is fast, compressed RAM-backed swap — bias the kernel to use it readily
# (Arch Wiki zram tuning). Filename sorts AFTER 99-performance.conf so it wins
# the vm.swappiness override (that file sets a conservative 10, wrong for zram).
vm.swappiness = 180
vm.page-cluster = 0
