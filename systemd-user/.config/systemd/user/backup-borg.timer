[Unit]
Description=Daily borg backup when the LUKS drive is connected

[Timer]
OnCalendar=daily
# catch up after the drive reconnects / a missed run
Persistent=true
RandomizedDelaySec=30m

[Install]
WantedBy=timers.target
